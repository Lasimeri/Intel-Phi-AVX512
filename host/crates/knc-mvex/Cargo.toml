[package]
name = "knc-mvex"
description = "Encoder for a subset of the Knights Corner vector ISA (MVEX prefix); the library the translator builds on"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
